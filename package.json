{
  "name": "dexter-run",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "bun run server.ts",
    "frontend": "cd frontend && bun run dev",
    "dev": "bun run server.ts & cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "deploy": "cd frontend && bun run build && cp -r dist/* /usr/share/nginx/html/",
    "start": "npx tsx server.ts & python3 dexter_bridge.py"
  },
  "dependencies": {
    "dexter-agent": "^0.1.0"
  }
}
